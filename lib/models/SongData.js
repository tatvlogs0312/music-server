function SongData(id, length, likeNumber, listen, name, national, year) {
    this.id = id,
    this.length = length,
    this.likeNumber = likeNumber,
    this.listen = listen,
    this.name = name,
    this.national = national,
    this.year = year
}

module.exports = SongData;